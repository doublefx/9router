# =============================================================================
# 9Router Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values
# Generate secrets with: openssl rand -base64 32

# -----------------------------------------------------------------------------
# REQUIRED SECRETS (Must be set before starting)
# -----------------------------------------------------------------------------

# JWT Secret for authentication tokens (REQUIRED)
# Generate with: openssl rand -base64 32
# Minimum 32 characters required
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# API Key Secret for HMAC generation (REQUIRED)
# Generate with: openssl rand -base64 32
# Used to generate and validate API keys with CRC signatures
API_KEY_SECRET=your_api_key_secret_here_minimum_32_characters

# -----------------------------------------------------------------------------
# OAuth Client Secrets (REQUIRED for OAuth providers)
# -----------------------------------------------------------------------------
# Get these from each provider's OAuth console
# IMPORTANT: Never commit actual secrets to git!

# Gemini/Gemini-CLI OAuth (Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
GEMINI_CLIENT_SECRET=your_gemini_secret_here

# iFlow OAuth
# https://iflow.cn/oauth
IFLOW_CLIENT_SECRET=your_iflow_secret_here

# Antigravity OAuth (Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
ANTIGRAVITY_CLIENT_SECRET=your_antigravity_secret_here

# Codex (OpenAI) OAuth - Only if using Codex provider
# https://platform.openai.com/account/api-keys
CODEX_CLIENT_SECRET=your_codex_secret_here

# -----------------------------------------------------------------------------
# Optional: Override OAuth Client IDs
# -----------------------------------------------------------------------------
# Uncomment and set these if you want to use custom OAuth client IDs
# (defaults are provided in code)

# GEMINI_CLIENT_ID=
# IFLOW_CLIENT_ID=
# ANTIGRAVITY_CLIENT_ID=

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------

# Allowed origins for API requests (comma-separated or single value)
# Default: http://localhost:3000,http://127.0.0.1:3000
ALLOWED_ORIGIN=http://localhost:3000

# Production origin (your deployed domain)
# Supports wildcard subdomains: *.9router.com
PRODUCTION_ORIGIN=https://9router.com

# Examples:
# ALLOWED_ORIGIN=http://localhost:3000,http://localhost:8080
# PRODUCTION_ORIGIN=*.yourdomain.com

# -----------------------------------------------------------------------------
# SECURITY NOTES
# -----------------------------------------------------------------------------
# - Never commit .env file to git
# - Use strong, random secrets (minimum 32 characters)
# - Rotate secrets regularly
# - Use different secrets for dev/staging/production

# -----------------------------------------------------------------------------
# Additional Configuration (will be added by remaining security fixes)
# -----------------------------------------------------------------------------
# Machine ID Salt, etc. will be added here
